{
  "name": "forkids-workshop",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview",
    "zip": "pnpm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\"",
    "format": "oxfmt .",
    "lint": "oxlint ."
  },
  "dependencies": {
    "kaplay": "3001.0.19"
  },
  "devDependencies": {
    "@types/node": "24.10.1",
    "oxfmt": "0.16.0",
    "oxlint": "1.31.0",
    "vite": "7.2.6"
  }
}
